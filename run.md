# run
> sudo service mysql start

> cd ~/git/packages-app/adminer && php -S 127.0.0.1:9000

> cd ~/git/packages-app/wp-app && wp server

> cd ~/git/packages-app/laravel-app && php artisan serve

> cd ~/git/packages-app/laravel-app && composer run dev